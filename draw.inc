
UpdatePlayer proto, xPos:byte, yPos:byte, dir:byte

DrawPlayer proto, xPos:byte, yPos:byte

TakeSpine proto, xPos:byte, yPos:byte

DrawCactus proto, xPosCacti:byte, yPosCacti:byte

UpdateCactus proto, cactiMem:dword, yPosCacti:byte, numCacti:byte

Grave proto, row:word, col:word

DrawStars proto
